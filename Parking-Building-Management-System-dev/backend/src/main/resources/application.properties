# Server Configuration
server.port=5000

# Spring Configuration
spring.main.banner-mode=off

# SQLite Database Properties
# We will use raw JDBC through DatabaseConnection, but we can document properties here
database.url=jdbc:sqlite:parking_building.db
